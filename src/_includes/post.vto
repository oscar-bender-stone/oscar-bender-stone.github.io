---
layout: layout.vto
---

<nav class="post-nav-top">
  <a href="/blog/" class="back-link">&larr; Back to Blog</a>
</nav>

<header class="post-header">
  <div class="post-meta-box">
    <div class="meta-line">
      <span class="meta-label">Published:</span>
      <time datetime="{{ date.toISOString().split('T')[0] }}" class="meta-value">
        {{ date.toLocaleDateString("en-US", { timeZone: "UTC", month: "long", day: "numeric", year: "numeric" }) }}
      </time>
    </div>

    <div class="meta-line">
      <span class="meta-label">Reading time:</span>
      {{ readingInfo.minutes }} {{ readingInfo.minutes == 1 ? "minute" : "minutes" }}
    </div>

    {{ if modified }}
      <div class="meta-line meta-updated">
        <span class="meta-label">Last updated:</span>
        {{ if modified_url }}
          <a href="{{ modified_url }}" class="modified-link" target="_blank" rel="noopener">
            {{ modified |> date("MMMM d, yyyy") }} ↗
          </a>
        {{ else }}
          <time datetime="{{ modified |> date('yyyy-MM-dd') }}">{{ modified |> date("MMMM d, yyyy") }}</time>
        {{ /if }}
      </div>
    {{ /if }}
  </div>
</header>

<article class="post-content">
  {{ content }}
</article>

<nav class="post-nav-bottom">
  <a href="/blog/" class="back-link">&larr; Back to Blog</a>
</nav>
